<%= render partial: "/shared/header" %>

<div class="c-devise-form__container">
    
  <h1>ユーザー登録</h1>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        
    <div class="c-devise-form__error-space">
      <%= render "devise/shared/error_messages", resource: resource %>
    </div>

    <div class="c-devise-form__section">
      <div class="c-devise-form__title">
        <%= f.label :username,"ユーザー名(15文字以内)" %><br />
      </div>
      <%= f.text_field :username, autofocus: true, autocomplete: "username", class: "c-devise-form__input" %>
    </div>

    <div class="c-devise-form__section">
      <div class="c-devise-form__title">
        <%= f.label :email,"メールアドレス" %><br />
      </div>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "c-devise-form__input" %>
    </div>

    <div class="c-devise-form__section">
      <div class="c-devise-form__title">
        <%= f.label :password,"パスワード(6文字以上の半角英数字)" %><br />
      </div>
      ＊英字と数字の両方を含めて設定してください。<br />
      <%= f.password_field :password, autocomplete: "new-password", class: "c-devise-form__input" %>
    </div>

    <div class="c-devise-form__section">
      <div class="c-devise-form__title">
        <%= f.label :password_confirmation,"パスワード再入力" %><br />
      </div>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "c-devise-form__input" %>
    </div>

    <div class="c-devise-form__btn-section">
      <%= f.submit "新規登録", class: "c-devise-form__btn"%>
    </div>
  <% end %>

</div>