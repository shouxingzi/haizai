<%= form_with model: form, id: "form", local: true do |f| %>
  
  <%= render 'shared/error_messages', model: f.object %>

  <div class="field">
    <%= f.label :title, "タイトル" %><br />
    <%= f.text_area :title, id:"title"%>
  </div>

  <div class="field">
    <%= f.label :text, "投稿文" %><br />
    <%= f.text_area :text, id:"text"%>
  </div>


  <div class="field">
    <div><%= f.label :prefecture_id, "提供場所（都道府県)" %></div>
    <div><%= f.collection_select :prefecture_id, Prefecture.all, :id, :prefecture, {include_blank: "都道府県を選択して下さい"}, {id: "prefecture-id"} %></div>
  </div>

  <div class="field">
    <div><%= f.label :city_id, "提供場所（市区町村)" %></div> 
    <div><%= f.collection_select :city_id, City.all, :id, :city, {include_blank: "市区町村を選択してください"}, {id: "city-id"}%></div>
  </div>

  <div class="tag-field", id='tag-field'>
    <div><%= f.label :name, "タグ" %></div>
    タグとタグの間は半角カンマ「,」で区切ってください。１０個まで登録できます。
    <div><%= f.text_field :name, {class: "input-tag", id: "form-tag-name"} %></div>
  </div>
  
  <div id="search-result">
  </div>

  <div class="field">
    <%= f.label :image, "画像" %><br />
    <%= f.file_field :image %>
  </div>

  <div id="image-list">
  </div>

  <div class="actions">
    <%= f.submit "送信" %>
  </div>


<% end %>


